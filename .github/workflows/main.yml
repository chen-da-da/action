name: GitHub Actions
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
    - name: test1
      run: |
        echo "start"
        echo "clone"
        git clone https://github.com/coolsnowwolf/lede
        echo "clone done"
        cd lede
        ls
